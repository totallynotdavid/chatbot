node_modules
.env
.env.*
.cloudflare-url

# build and dev generated files
*.tsbuildinfo
dist
logs

# private asset storage (signed contracts, call recordings) - generated at
# runtime and by tests, never source. `data/uploads` is deliberately not here:
# the seeded catalog images under it are tracked.
**/data/private/
**/data/contracts/

# database
*.sqlite
*.sqlite-shm
*.sqlite-wal

# dev tools
*.ps1
example.md
