[test]
# `bun test` from the repository root runs the backend suite too, and that suite
# drives the application's own database connection. The preload points DB_PATH
# at a throwaway file before anything opens it.
preload = ["./apps/backend/tests/setup.ts"]
